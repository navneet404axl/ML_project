# ML_project

To run the notebook files (`.ipynb`), keep `diabetes.csv` in the same root folder as the notebooks. The Pima Diabetes notebook expects to load the dataset from the project root, so moving the CSV into another folder may cause a file-not-found error.
